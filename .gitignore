# Python bytecode / caches
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/

# Build / packaging artifacts
build/
dist/
*.egg-info/
unsloth_compiled_cache/

# Test / typecheck / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pyright/

# Notebook and editor noise
.ipynb_checkpoints/
.DS_Store

# Experiment outputs — full run trees are large and are mirrored to HF
# (arcadia-impact/matching-game-scale-logs). Keep them out of git.
runs/
*.json

.env

# arch2 session state (local-only)
.arch/.session.json

# arch2 public iteration data (must be committed despite *.json)
!data/public/heldout.json
