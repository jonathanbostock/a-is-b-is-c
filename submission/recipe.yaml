# Attempt 47 — combine best ideas: L2-SP 2e-2 + FROZEN ATTENTION.
# Champion #43 (full-param ALL weights + L2-SP 2e-2, 1000 steps) = test 0.792 /
# retention 0.834 / 0.660, retention-limited. My other strong idea (#90) froze
# attention (the decisiveness carrier) for structural protection. Combine: freeze
# attention (structural decisiveness protection) AND L2-SP 2e-2 on the trainable
# MLP (weight-space protection). If the two protections stack, retention could
# exceed 0.834 while the full-rank MLP still crystallizes near the ceiling ->
# score past 0.66. Risk: freezing attention caps test. lr 1e-4, 1000 steps.
use_lora: false
lora_r: 0
lr: 0.0001
num_steps: 1000
eval_every: 1000
warmup_ratio: 0.03
batch_size: 8
grad_accum: 2
max_seq_length: 128
gradient_checkpointing: true
attn_implementation: sdpa
dense_early_evals: false
collect_residuals: false
eval_subsample: 0
n_repeats: 1
save_final: true
paged_adamw_8bit: true
freeze_embeddings: true
freeze_attention: true
l2_sp_lambda: 0.02
max_grad_norm: 1.0
