# Attempt: mlp-r88-rh25 (final safe LoRA band draw)
# Last distinct at-cap held-out draw in the winning MLP-only LoRA band. rank 88 with
# rehearsal 0.25 is an untried combo. #78 (rank 96) leads my held-out at 0.5614.
use_lora: true
lora_r: 88
lora_alpha: 176
lora_dropout: 0.05
lora_target_modules: [gate_proj, up_proj, down_proj]
lr: 3.0e-4
num_steps: 400
eval_every: 200
batch_size: 16
grad_accum: 2
max_seq_length: 128
weight_decay: 0.0
mixin_jsonl: attempts/mlp-r88-rh25/mixin.jsonl
mixin_ratio: 0.25
attn_implementation: sdpa
gradient_checkpointing: true
dense_early_evals: false
eval_subsample: 48
