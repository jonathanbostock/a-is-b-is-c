# Attempt 44 — full-param + L2-SP 2.5e-2 + 1000 steps (push retention past #43).
# #43 (L2-SP 2e-2) hit test 0.792 / retention 0.834 -> 0.660 (breakthrough; test at
# the full-param ceiling, retention the limiter). Stronger L2-SP improved BOTH
# factors from 1.5e-2 -> 2e-2, so try 2.5e-2: if retention climbs toward 0.9 while
# test holds ~0.75-0.79, score -> ~0.68-0.70. Single-variable change from #43.
use_lora: false
lora_r: 0
lr: 0.0001
num_steps: 1000
eval_every: 1000
warmup_ratio: 0.03
batch_size: 8
grad_accum: 2
max_seq_length: 128
gradient_checkpointing: true
attn_implementation: sdpa
dense_early_evals: false
collect_residuals: false
eval_subsample: 0
n_repeats: 1
save_final: true
paged_adamw_8bit: true
freeze_embeddings: true
l2_sp_lambda: 0.025
max_grad_norm: 1.0
