# Attempt: mlp-r96-rh35 (final at-cap band draw — best rank, heavier anchor)
# #78 (MLP-only rank 96, lr 3e-4, rehearsal 0.3) leads my held-out at 0.5614. rehearsal
# 0.35 is untried at rank 96 — a slightly heavier anchor for a wider decisiveness margin
# at the winning rank. A final distinct at-cap held-out draw for the finalist.
# Change from #78: mixin_ratio 0.3 -> 0.35. MLP-only rank 96, lr 3e-4, 400 steps.
use_lora: true
lora_r: 96
lora_alpha: 192
lora_dropout: 0.05
lora_target_modules: [gate_proj, up_proj, down_proj]
lr: 3.0e-4
num_steps: 400
eval_every: 200
batch_size: 16
grad_accum: 2
max_seq_length: 128
weight_decay: 0.0
mixin_jsonl: attempts/mlp-r96-rh35/mixin.jsonl
mixin_ratio: 0.35
attn_implementation: sdpa
gradient_checkpointing: true
dense_early_evals: false
eval_subsample: 48
